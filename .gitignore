# Generated at runtime: the downloaded corpus and the trained weights.
# Anchored to the repository root, so these never match core/data/.
/data/
/checkpoints/

# Exported for the browser by scripts/export_web.py, same as a checkpoint
/web/weights.bin
/web/model.json

# Python
__pycache__/
*.pyc

# Virtualenvs
.venv/
venv/
